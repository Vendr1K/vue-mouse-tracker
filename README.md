# Vue.js Плагин для отслеживания координат курсора мыши

Плагин для Vue 3, который собирает данные о положении курсора мыши на странице и отправляет их на сервер. Координаты рассчитываются относительно указанного HTML-элемента.

## Установка

1. **Установить плагин из GitHub**

   ```bash
   npm install Vendr1K/vue-mouse-tracker
   ```

2. **Плагин для Vue 3**:

   - Плагин разработан специально для Vue 3
   - Использует современный API Vue 3 (app.directive, mounted, unmounted, app.config.globalProperties)
   - Не требует дополнительных адаптеров

3. **Подключен плагин в `main.js`**:

   - Плагин установлен глобально через `app.use()`
   - Настроены глобальные параметры по умолчанию
   - Импортируется напрямую из `vue-mouse-tracker`

4. **Добавлен трекинг в `TheWelcome.vue`**:
   - Использована директива `v-track-coords`
   - Добавлено отображение координат в реальном времени

## Особенности

- ✅ Сбор координат курсора каждые 30мс (настраивается)
- ✅ Автоматическая отправка данных на сервер каждые 3 секунды (настраивается)
- ✅ Координаты рассчитываются относительно указанного элемента

## 🔧 Настройки плагина:

1. **checkInterval** (30 мс) - период сбора данных

- Плагин записывает координаты, если курсор находится в одной точке ≥ 30 мс

2. **sendInterval** (3000 мс) - период отправки данных на сервер

- Каждые 3 секунды собранные данные отправляются POST-запросом

3. **url** ('/api/track') - адрес для отправки данных

- ⚠️ **ВАЖНО**: Измените на ваш реальный URL сервера!

## 📊 Формат отправляемых данных:

```json
{
  "coordinates": [
    { "x": 100, "y": 150, "time": 45 },
    { "x": 120, "y": 160, "time": 30 }
  ],
  "timestamp": 1234567890,
  "elementId": null
}
```

## 📚 Дополнительная информация по инициализации + тестирование работы :

См. `example` в корне проекта.
